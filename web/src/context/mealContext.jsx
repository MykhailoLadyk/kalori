// will also run service/updateMeals
// exports updateMeals
// - fetches today's meals on mount via mealService.fetchMeals()
// - state: meals, loading
// - exposes: meals, addMeal(), deleteMeal(), editMeal()
// - each action calls mealService + updates local state
