// - props: meals (grouped by type), onEdit, onDelete
// - maps over meal types, renders category header + MealCards
// - owns nothing
// handles rendering breakfast/lunch/dinner/snack sections on home page
