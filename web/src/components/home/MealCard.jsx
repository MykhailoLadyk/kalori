// - props: meal, color, onEdit, onDelete
// - owns expanded state (local UI only)
// - renders macro pills, expand animation, edit/delete buttons
// - fires onEdit(id) and onDelete(id) upward when buttons clicked(it also opens modals)
