// Streak element
// time period element
// statsItemQuick.jsx x4(3 props, chagne depending on time period at stats.jsx level)
// 3x statsBarChart.jsx / maybe change to graph depedning on time period?
// statsMacroSplit.jsx

// - imports StatsBarChart, StatsItemQuick, StatsMacroSplit
// - calls useStats(logs) to get avgCalories, streak, deficit
// - owns period state ("W" | "M" | "3M") (All the values based on it)
// - passes period and derived stats down as props
// children are just UI
