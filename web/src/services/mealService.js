  // - fetchMeals(date)        GET /api/meals?date=
  // - addMeal(meal)           POST /api/meals
  // - updateMeal(id, meal)    PUT /api/meals/:id
  // - deleteMeal(id)          DELETE /api/meals/:id